# ผลการทดสอบอัตโนมัติ — LeaveEasy

**รันเมื่อ:** 20 กันยายน 2026 · ใช้เวลา 39.9 วินาที
**เครื่องมือ:** Playwright (chromium) ผ่าน `npm test`
**ระบบที่ทดสอบ:** http://localhost:3100 (โค้ดชุดเดียวกับที่ deploy ขึ้น https://leaveeasy-oneshot-anuphan.web.app)

## สรุป

| | จำนวน |
|---|---|
| ✅ ผ่าน | 2 |
| ❌ ไม่ผ่าน | 2 |
| ⏭ ไม่ได้รัน | 1 |
| **รวม** | **5** |

> ⚠️ **รายงานนี้เขียนตามผลจริง ไม่ได้แก้ให้ดูเหมือนผ่าน**

---

## ✅ เทสต์ 3 — เว้นช่องหัวข้อว่างแล้วกดบันทึก ต้องไม่สร้างใบลา (US-02)

**ตรวจอะไร:** กรอกทุกช่องยกเว้นหัวข้อ → กดบันทึก → ต้องไม่ถูกพาไปหน้ารายการ
ช่องหัวข้อต้องอยู่ในสถานะไม่ผ่านการตรวจและมีข้อความเตือน และจำนวนแถวในหน้ารายการต้องไม่เพิ่มหลังโหลดหน้าใหม่

**ผล:** **ผ่าน**

> ข้อความที่ปฏิเสธมาจาก `required` ของ HTML5 คือ**เบราว์เซอร์เป็นคนปฏิเสธ**ตั้งแต่ก่อนโค้ดของแอปได้ทำงาน
> เป็นพฤติกรรมมาตรฐาน ไม่ใช่บั๊ก

## ✅ เทสต์ 4 — ยังไม่ล็อกอิน เปิดหน้ารายการใบลา ต้องอ่านข้อมูลไม่ได้เลย (US-08)

**ตรวจอะไร:** วัดที่ตัวข้อมูลเอง ไม่ใช่วัดว่าหน้าจอเด้งไปไหน — ยิง `getDocs(collection(db,'leaveRequests'))`
ตรงผ่าน Firestore SDK ข้ามหน้าเว็บไปเลย แล้วต้องได้ error `permission-denied`

**ผล:** **ผ่าน** — Firestore ปฏิเสธด้วย `permission-denied` จริง
ยืนยันว่า `firestore.rules` ที่ deploy ขึ้น Console แล้วทำงานจริง

---

## ❌ เทสต์ 1 — ยื่นใบลาแล้วไปโผล่ในรายการ พร้อมสถานะรอพิจารณา (US-01, US-02)

**ผล:** **ไม่ผ่าน**
**ข้อความ:** `ต้องมีประเภทการลาอย่างน้อย 1 รายการให้เลือก (โหลดจาก leaveTypes)` — พบตัวเลือก 1 รายการ คาดว่าต้องมากกว่า 1

**สาเหตุ:** โฟลเดอร์ `leaveTypes` บน Firestore **ยังว่างเปล่า** รายการเลื่อนลงจึงมีแต่ตัวเลือกหัวข้อ
`— เลือกประเภทการลา —` อย่างเดียว เทสต์เลือกประเภทไม่ได้จึงยื่นใบลาไม่ได้

**ไม่ใช่ข้อบกพร่องของโค้ดและไม่ใช่ข้อบกพร่องของเทสต์** — เป็นเรื่องข้อมูลตั้งต้นที่ยังไม่ได้ใส่
แก้ได้โดยเข้าสู่ระบบด้วยบัญชี `hr` แล้วเพิ่มประเภทการลาที่หน้า `leave-types.html` อย่างน้อย 1 รายการ
หรือรัน `seed.html` ครั้งเดียว

## ⏭ เทสต์ 2 — ผู้อนุมัติกดอนุมัติแล้วสถานะเปลี่ยนเป็นอนุมัติ (US-04)

**ผล:** **ไม่ได้รัน** — ใช้ใบลาที่เทสต์ 1 สร้าง พอเทสต์ 1 ตก ตัวนี้จึงถูกข้าม

## ❌ เทสต์ 5 — ผู้ใช้ที่ไม่ใช่เจ้าของเปิด URL ใบลาของคนอื่นตรง ๆ ต้องเข้าไม่ได้ (US-08)

**ผล:** **ไม่ผ่าน** — ไม่พบข้อความ `เปิดใบขอลานี้ไม่ได้`

**สาเหตุ: ระบบทำถูกตามสเปก แต่ข้อมูลบัญชีทดสอบไม่ครบ**
ตอนนี้มีบัญชีจริงเพียงใบเดียวและเป็นบทบาท `hr` จึงต้องใส่บัญชีเดียวกันทั้งช่องเจ้าของและช่องผู้บุกรุก
แต่ `firestore.rules` อนุญาตให้ `manager` และ `hr` เปิดใบลาของทุกคนได้ตามสเปก US-08 อยู่แล้ว
ผู้บุกรุกในเทสต์จึงเข้าได้อย่างถูกต้อง เทสต์จึงตก

**นี่ไม่ใช่ช่องโหว่ความปลอดภัย** และไม่ได้แก้เทสต์ให้ผ่าน เพราะนั่นคือการปลอมผล
ต้องสมัครบัญชีที่สองผ่าน `signup.html` (ได้บทบาท `employee` อัตโนมัติ) แล้วใส่เป็นช่องผู้ขอลา จึงจะทดสอบได้จริง

---

## สิ่งที่ต้องทำเพื่อให้ครบ 5 ข้อ

1. เข้าสู่ระบบด้วยบัญชี `hr` → เพิ่มประเภทการลาที่ `leave-types.html` อย่างน้อย 1 รายการ → ปลดล็อกเทสต์ 1 และ 2
2. สมัครบัญชีที่สองผ่าน `signup.html` แล้วใส่ลง `tests/accounts.json` ช่อง `ผู้ขอลา` → ปลดล็อกเทสต์ 5
3. `npm test`

รายการที่ยังค้างบันทึกไว้ใน [BACKLOG.md](BACKLOG.md)
