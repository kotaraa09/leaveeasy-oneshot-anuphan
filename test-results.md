# ผลการทดสอบอัตโนมัติ — LeaveEasy

**รันเมื่อ:** 20 กันยายน 2026 · ใช้เวลา 31.8 วินาที
**เครื่องมือ:** Playwright (chromium) ผ่าน `npm test`
**ระบบที่ทดสอบ:** http://localhost:3100 (โค้ดชุดเดียวกับที่ deploy ขึ้น https://leaveeasy-oneshot-anuphan.web.app)
**บัญชีที่ใช้:** ผู้ขอลา บทบาท `employee` · ผู้อนุมัติ บทบาท `hr`
(เก็บใน `tests/accounts.json` ซึ่ง `.gitignore` กันไว้ ไม่ขึ้น GitHub)

## สรุป

| | จำนวน |
|---|---|
| ✅ ผ่าน | 3 |
| ❌ ไม่ผ่าน | 1 |
| ⏭ ไม่ได้รัน | 1 |
| **รวม** | **5** |

> ⚠️ **รายงานนี้เขียนตามผลจริง ไม่ได้แก้ให้ดูเหมือนผ่าน และไม่ได้แก้เทสต์เพื่อให้ผ่าน**

---

## ✅ เทสต์ 3 — เว้นช่องหัวข้อว่างแล้วกดบันทึก ต้องไม่สร้างใบลา (US-02)

**ตรวจอะไร:** กรอกทุกช่องยกเว้นหัวข้อ → กดบันทึก → ต้องไม่ถูกพาไปหน้ารายการ · ช่องหัวข้อต้องไม่ผ่าน
การตรวจและมีข้อความเตือน · จำนวนแถวในหน้ารายการต้องไม่เพิ่มหลังโหลดหน้าใหม่

**ผล:** ✅ **ผ่าน** (5.8 วินาที)

> ข้อความที่ปฏิเสธคือ `Please fill out this field.` ซึ่ง **มาจากเบราว์เซอร์** ผ่าน `required` ของ HTML5
> ไม่ใช่ข้อความที่โค้ดของแอปเขียนเอง เพราะ `required` บล็อกตั้งแต่ก่อน event `submit` จะยิง
> เป็นพฤติกรรมมาตรฐาน ไม่ใช่บั๊ก และยังนับว่า "ระบบไม่รับ + มีข้อความบอก" ตามเกณฑ์ US-02

## ✅ เทสต์ 4 — 🔒 ยังไม่ล็อกอิน เปิดหน้ารายการใบลา ต้องอ่านข้อมูลไม่ได้เลย (US-08)

**ตรวจอะไร:** วัดที่ตัวข้อมูลเอง ไม่ใช่วัดว่าหน้าจอเด้งไปไหน — ยิง `getDocs(collection(db,'leaveRequests'))`
ตรงผ่าน Firestore SDK ข้ามหน้าเว็บไปเลย แล้วต้องได้ error `permission-denied`

**ผล:** ✅ **ผ่าน** (1.9 วินาที) — Firestore ปฏิเสธด้วย `permission-denied` จริง

## ✅ เทสต์ 5 — 🔒 ผู้ใช้ที่ไม่ใช่เจ้าของเปิด URL ใบลาของคนอื่นตรง ๆ ต้องเข้าไม่ได้ (US-08)

**ตรวจอะไร:** ผู้อนุมัติ (`hr`) ยื่นใบลาของตัวเองไว้ 1 ใบ → ออกจากระบบ → ผู้ขอลา (`employee`
ซึ่งไม่ใช่เจ้าของและไม่ใช่ `manager`/`hr`) เปิด URL ของใบนั้นตรง ๆ → ต้องเข้าไม่ได้
วัดด้วยการยิง `getDoc` ผ่าน SDK แล้วต้องได้ `permission-denied`

**ผล:** ✅ **ผ่าน** (16.2 วินาที)

- เตรียมใบลา `id=olroH9YAwwWqHClzCV6Q` สำเร็จ
- ผู้ขอลาเปิดไม่ได้จริง ทั้งทางหน้าเว็บและทางการยิงอ่านฐานข้อมูลตรง ๆ
- **เก็บกวาดสำเร็จ** ลบใบลาที่สร้างไว้ออกจากฐานข้อมูลแล้ว ไม่มีข้อมูลทดสอบค้าง

> เทสต์นี้สลับบทบาทจากที่ออกแบบไว้ตอนแรก เพราะ `firestore.rules` อนุญาตให้ `manager` และ `hr`
> เปิดใบลาของทุกคนได้ตามสเปก US-08 อยู่แล้ว ถ้าใช้ `hr` เป็นผู้บุกรุก เทสต์จะตกทั้งที่ระบบทำถูก

---

## ❌ เทสต์ 1 — ยื่นใบลาแล้วไปโผล่ในรายการ พร้อมสถานะรอพิจารณา (US-01, US-02)

**ตรวจอะไร:** ล็อกอินเป็นผู้ขอลา → กรอกฟอร์มครบ → บันทึก → ใบใหม่ต้องขึ้นในหน้ารายการ
แล้ว **โหลดหน้าใหม่ตรวจซ้ำ** ว่ายังอยู่ เพื่อพิสูจน์ว่าลงฐานจริง

**ผล:** ❌ **ไม่ผ่าน** (2.8 วินาที)
**ข้อความ:** `ต้องมีประเภทการลาอย่างน้อย 1 รายการให้เลือก (โหลดจาก leaveTypes)` — พบตัวเลือก 1 รายการ

**สิ่งที่สังเกตได้จริง ยังหาสาเหตุไม่จบ**

- เมื่อล็อกอินด้วยบัญชี **`employee`** รายการเลื่อนลงประเภทการลามีแต่ `— เลือกประเภทการลา —`
- เมื่อล็อกอินด้วยบัญชี **`hr`** (ในเทสต์ 5 รอบเดียวกัน) รายการเลื่อนลงมีประเภทให้เลือกและยื่นใบลาสำเร็จ
- หน้าที่ตก **ไม่มีข้อความ** `โหลดรายการประเภทการลาไม่สำเร็จ` ปรากฏ แปลว่า `getDocs` ไม่ได้โยน error
  แต่คืนรายการว่าง — ไม่ใช่การถูกปฏิเสธสิทธิ์แบบปกติซึ่งจะโยน `permission-denied` ออกมา
- รันซ้ำอีกรอบได้ผลเหมือนเดิมทุกประการ จึงไม่ใช่เรื่องจังหวะเวลาที่ข้อมูลยังไม่ถูกเพิ่ม

**ยังไม่สรุปว่าเป็นบั๊กของโค้ด** เพราะกฎใน `firestore.rules` เขียนไว้ว่า `leaveTypes` อ่านได้ถ้าล็อกอินแล้ว
ซึ่งครอบคลุมบทบาท `employee` อยู่ ต้องตรวจต่อว่ากฎที่ deploy ขึ้น Console จริงตรงกับไฟล์ในเรโปหรือไม่

**วิธียืนยันด้วยตาเอง 10 วินาที:** เข้า https://leaveeasy-oneshot-anuphan.web.app/new-leave-request.html
ด้วยบัญชีบทบาท `employee` แล้วดูว่ารายการเลื่อนลงประเภทการลาว่างจริงไหม
ถ้าว่าง แปลว่าเป็นพฤติกรรมของระบบจริง ไม่ใช่ผลข้างเคียงของชุดทดสอบ

## ⏭ เทสต์ 2 — ผู้อนุมัติกดอนุมัติแล้วสถานะเปลี่ยนเป็นอนุมัติ (US-04)

**ตรวจอะไร:** ล็อกอินเป็นผู้อนุมัติ → เปิดใบที่ `รอพิจารณา` → กดอนุมัติ → สถานะเปลี่ยนเป็น `อนุมัติ`
แล้ว **โหลดหน้ารายละเอียดใหม่ตรวจซ้ำ**

**ผล:** ⏭ **ไม่ได้รัน** — ใช้ใบลาที่เทสต์ 1 สร้าง พอเทสต์ 1 ตก ตัวนี้จึงถูกข้ามโดยอัตโนมัติ

---

รายการที่ยังค้างบันทึกไว้ใน [BACKLOG.md](BACKLOG.md)

```bash
npm test      # รันซ้ำได้ด้วยคำสั่งเดียว
```
