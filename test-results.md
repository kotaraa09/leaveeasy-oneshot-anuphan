# ผลการทดสอบอัตโนมัติ — LeaveEasy

**รันเมื่อ:** 20 กันยายน 2026
**เครื่องมือ:** Playwright (chromium) ผ่าน `npm test`
**ระบบที่ทดสอบ:** http://localhost:3100 (โค้ดชุดเดียวกับที่ deploy ขึ้น https://leaveeasy-oneshot-anuphan.web.app)

## สรุป

| | จำนวน |
|---|---|
| ✅ ผ่าน | 1 |
| ❌ ไม่ผ่าน | 3 |
| ⏭ ไม่ได้รัน | 1 |
| **รวม** | **5** |

> ⚠️ **รายงานนี้เขียนตามผลจริง ไม่ได้แก้ให้ดูเหมือนผ่าน**
> เทสต์ที่ไม่ผ่านทั้ง 3 ตัวตกที่จุดเดียวกันคือ **ขั้นล็อกอิน** ไม่ใช่ที่ตรรกะของระบบ

---

## รายละเอียดรายข้อ

### ✅ เทสต์ 4 — ยังไม่ล็อกอิน เปิดหน้ารายการใบลา ต้องอ่านข้อมูลไม่ได้เลย (US-08)

**ตรวจอะไร:** คนที่ไม่ได้ล็อกอินต้องอ่านโฟลเดอร์ `leaveRequests` ไม่ได้ โดยวัดที่ตัวข้อมูลเอง
ไม่ใช่วัดว่าหน้าจอเด้งไปไหน — เทสต์ยิง `getDocs(collection(db,'leaveRequests'))` ตรงผ่าน Firestore SDK
ข้ามหน้าเว็บไปเลย แล้วต้องได้ error `permission-denied`

**ผล:** **ผ่าน** — Firestore ปฏิเสธด้วย `permission-denied` จริง
ยืนยันว่า `firestore.rules` ที่ deploy ขึ้น Console แล้วทำงานจริง ไม่ใช่กฎเก่า

---

### ❌ เทสต์ 1 — ยื่นใบลาแล้วไปโผล่ในรายการ พร้อมสถานะรอพิจารณา (US-01, US-02)

**ตรวจอะไร:** ล็อกอินเป็นผู้ขอลา → กรอกฟอร์มครบ → บันทึก → ใบใหม่ต้องขึ้นในหน้ารายการ
แล้ว **โหลดหน้าใหม่ตรวจซ้ำ** ว่ายังอยู่ เพื่อพิสูจน์ว่าลงฐานจริง

**ผล:** **ไม่ผ่าน** — ตกตั้งแต่ขั้นล็อกอิน ไม่ได้ไปถึงการทดสอบตรรกะ
**เหตุผล:** หน้าเข้าสู่ระบบขึ้นข้อความ `⚠️ อีเมลหรือรหัสผ่านไม่ถูกต้อง`
บัญชีใน `tests/accounts.json` ยังเป็นค่าตัวอย่าง `employee@example.com` ซึ่งไม่มีอยู่จริงใน Firebase Authentication

---

### ⏭ เทสต์ 2 — ผู้อนุมัติกดอนุมัติแล้วสถานะเปลี่ยนเป็นอนุมัติ (US-04)

**ตรวจอะไร:** ล็อกอินเป็นผู้อนุมัติ → เปิดใบที่ `รอพิจารณา` → กดอนุมัติ → สถานะเปลี่ยนเป็น `อนุมัติ`
แล้ว **โหลดหน้ารายละเอียดใหม่ตรวจซ้ำ**

**ผล:** **ไม่ได้รัน** — เทสต์นี้ใช้ใบลาที่เทสต์ 1 สร้างไว้ พอเทสต์ 1 ตก ตัวนี้จึงถูกข้าม

---

### ❌ เทสต์ 3 — เว้นช่องหัวข้อว่างแล้วกดบันทึก ต้องไม่สร้างใบลา และมีข้อความเตือน (US-02)

**ตรวจอะไร:** กรอกทุกช่องยกเว้นหัวข้อ → กดบันทึก → ต้องไม่ถูกพาไปหน้ารายการ
ช่องหัวข้อต้องอยู่ในสถานะไม่ผ่านการตรวจและมีข้อความเตือน และจำนวนแถวในหน้ารายการต้องไม่เพิ่ม

**ผล:** **ไม่ผ่าน** — ตกที่ขั้นล็อกอินเช่นเดียวกับเทสต์ 1

> 📌 หมายเหตุที่ควรรู้ไว้: ช่องหัวข้อใช้ `required` ของ HTML5 ดังนั้น**เบราว์เซอร์เป็นคนปฏิเสธและขึ้นข้อความ**
> ตั้งแต่ก่อนที่โค้ดของแอปจะได้ทำงาน ซึ่งเป็นพฤติกรรมมาตรฐาน ไม่ใช่บั๊ก

---

### ❌ เทสต์ 5 — ผู้ใช้ที่ไม่ใช่เจ้าของเปิด URL ใบลาของคนอื่นตรง ๆ ต้องเข้าไม่ได้ (US-08)

**ตรวจอะไร:** ให้ผู้อนุมัติยื่นใบลาของตัวเองไว้ 1 ใบ แล้วให้ผู้ขอลา (ไม่ใช่เจ้าของ ไม่ใช่ `manager`/`hr`)
เปิด URL ของใบนั้นตรง ๆ → ต้องเข้าไม่ได้ วัดด้วยการยิง `getDoc` ผ่าน SDK แล้วต้องได้ `permission-denied`

**ผล:** **ไม่ผ่าน** — ตกที่ขั้นล็อกอิน ยังไม่ได้ทดสอบกฎความปลอดภัยข้อนี้จริง

> เทสต์นี้สลับบทบาทจากที่วางแผนไว้ตอนแรก เพราะ `firestore.rules` อนุญาตให้ `manager`/`hr`
> เปิดใบลาของทุกคนได้ตามสเปก US-08 อยู่แล้ว ถ้าใช้ `manager` เป็นผู้บุกรุก เทสต์จะตกทั้งที่ระบบทำถูก

---

## สิ่งที่ต้องทำเพื่อให้เทสต์ที่เหลือรันได้

แก้ `tests/accounts.json` (ไฟล์นี้ถูก `.gitignore` กันไว้ ไม่ขึ้น GitHub) ให้ใส่อีเมลและรหัสผ่าน
ของบัญชีที่มีอยู่จริงใน **Firebase Console → Authentication → Users**
โดยต้องมีอย่างน้อย 1 บัญชีที่ `role` เป็น `manager` หรือ `hr` เพื่อให้เทสต์ 2 รันได้

```bash
npm test
```

รายการที่ยังค้างถูกบันทึกไว้ใน [BACKLOG.md](BACKLOG.md) แล้ว
