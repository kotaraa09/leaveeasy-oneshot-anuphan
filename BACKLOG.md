# BACKLOG — สิ่งที่ยังไม่ได้ทำ และเหตุผล

โครงงาน LeaveEasy · Lab 4 สัปดาห์ที่ 9 (สร้างรอบเดียวจากสเปก)

---

## 1. อยู่นอกขอบเขต Module 2 — สเปกระบุเองว่ายังไม่ทำ

| รายการ | อ้างอิง | ทำเมื่อไร |
|---|---|---|
| ค้นหาด้วยคำในหัวข้อ · ตัวกรองสถานะและประเภท · ปุ่มสลับการเรียงลำดับ | US-10 | Backlog Sprint 2 |
| แดชบอร์ดนับจากข้อมูลจริงและกราฟ — ตอนนี้เป็นโครงหน้าใส่เลขหลอก | US-11 · สเปกหัวข้อ 4 หน้าที่ 5 | Module 3 |
| แนบเอกสารประกอบใบลา (`attachmentUrl`) — ต้องใช้ Firebase Storage | US-12 · สเปกหัวข้อ 9 | Module 3 |
| มอบหมายผู้อนุมัติ — ตอนนี้ `approverId` / `approverName` เขียนเป็นค่าว่างตอนยื่นใบ | สเปกหัวข้อ 4 ไม่มีหน้าจอสำหรับเลือกผู้อนุมัติ | Sprint 2 |

---

## 2. ตัดออกตอนตรวจ A4 เพราะไม่มีในสเปก

**ช่อง `aiSuggestion` และโฟลเดอร์ย่อย `aiLog`**

ตอนสร้างระบบมีการใส่ช่อง `aiSuggestion` ไว้ในใบลา และโฟลเดอร์ย่อย `aiLog` ที่จดทุกครั้งที่เรียก AI
(`input` · `output` · `createdAt`) พร้อมกฎใน `firestore.rules` และการไล่ลบตอนลบใบลา

ตรวจกับ `leaveeasy-spec.md` หัวข้อ 5.2 แล้ว **ตารางช่องข้อมูลของ `leaveRequests` ไม่มีทั้งสองอย่าง**
และ US-09 ก็ไม่ได้ขอให้เก็บอะไรลงฐาน จึงตัดออกทั้งหมดตามขั้น A4 ของใบงาน

> 📌 **ของดีที่ควรกลับมาทำ** — ไม่เก็บ `input` ที่ส่งเข้า AI ไว้ แปลว่าตรวจย้อนหลังไม่ได้ว่า
> ครั้งนั้น AI เห็นอะไรจึงตอบแบบนั้น ถ้าสเปกรอบหน้าเปิดช่องให้ ควรใส่กลับ

---

## 3. 🔴 ความปลอดภัยที่ยังแก้ไม่จบ

**คีย์ OpenRouter ยังถูกส่งขึ้น Firebase Hosting**

`js/ai-key.js` ถูก `.gitignore` กันไม่ให้ขึ้น GitHub แล้ว (ยืนยันแล้ว) แต่**ไม่ได้อยู่ใน `ignore` ของ `firebase.json`**
เพราะถ้าไม่ส่งขึ้นไป เว็บจริงจะเรียก AI ไม่ได้ — แปลว่าใครกด F12 บนเว็บก็ยังเห็นคีย์

การแยกไฟล์กันได้แค่ **"คีย์หลุดขึ้น GitHub"** ไม่ได้ทำให้คีย์เป็นความลับ

**ทางแก้จริง:** ย้ายการเรียก AI ไปไว้ฝั่งที่ผู้ใช้แตะไม่ได้ (ตัวกลางฝั่งเซิร์ฟเวอร์ เช่น Cloud Functions)
แล้วให้หน้าเว็บยิงไปที่ตัวกลางแทน — อยู่นอกขอบเขต Module 2 ที่ระบุว่า "ไม่มีเซิร์ฟเวอร์ที่เขียนเอง"

---

## 4. ข้อจำกัดที่รู้ตัวและยอมรับไว้

| เรื่อง | รายละเอียด |
|---|---|
| **กฎ "ต้องมีความเห็นก่อนกดไม่อนุมัติ"** บังคับได้แค่ฝั่งหน้าเว็บ | Security Rules นับจำนวนไฟล์ในโฟลเดอร์ย่อยไม่ได้ ใครเปิด Console ของเบราว์เซอร์ก็ข้ามได้ |
| **ข้อมูลตัวอย่างไม่ผูกกับบัญชีจริง** | `u001`–`u003` และ `lr001`–`lr005` มี `requesterId` ที่ไม่ตรงกับ `uid` ของใครเลย พอ US-08 กรองด้วย `uid` ใบพวกนี้จะไม่ขึ้นให้ใครเห็น — ต้องตัดสินใจว่าจะชี้ไป `uid` จริง หรือลบทิ้ง |
| **ตอน seed ต้องเปิดกฎชั่วคราว** | ชื่อไฟล์ตัวอย่างไม่ตรงกับ `uid` จริง กฎจริงจึงปฏิเสธ — ต้องเปิด `allow read, write: if true;` ชั่วคราว แล้ว **เอากฎจริงกลับทันทีหลัง seed เสร็จ** |


---

## 5. เทสต์ที่ยังไม่ผ่าน (รันเมื่อ 20 ก.ย. 2026)

| เทสต์ | สถานะ | สาเหตุ |
|---|---|---|
| 4 · ยังไม่ล็อกอิน อ่านข้อมูลไม่ได้ | ✅ ผ่าน | — |
| 1 · ยื่นใบลาแล้วโผล่ในรายการ | ❌ | ตกที่ขั้นล็อกอิน |
| 3 · เว้นช่องหัวข้อว่าง | ❌ | ตกที่ขั้นล็อกอิน |
| 5 · เปิดใบลาของคนอื่น | ❌ | ตกที่ขั้นล็อกอิน |
| 2 · อนุมัติแล้วสถานะเปลี่ยน | ⏭ | ไม่ได้รัน เพราะต่อจากเทสต์ 1 |

**สาเหตุเดียวกันทั้งหมด:** `tests/accounts.json` ยังใส่อีเมลตัวอย่าง `employee@example.com`
และ `manager@example.com` ซึ่งไม่มีอยู่จริงใน Firebase Authentication หน้าเข้าสู่ระบบจึงขึ้น
`อีเมลหรือรหัสผ่านไม่ถูกต้อง` แล้วเทสต์รอการเปลี่ยนหน้าจนหมดเวลา

**ไม่ใช่ข้อบกพร่องของโค้ดระบบและไม่ใช่ข้อบกพร่องของเทสต์** — เป็นเรื่องข้อมูลบัญชีทดสอบ
แก้ได้ด้วยการใส่อีเมลและรหัสผ่านของบัญชีจริงลง `tests/accounts.json` แล้วรัน `npm test` ใหม่

## 6. งานออกแบบที่ยังค้างจากผลตรวจ

- **พื้นผ้าฝ้ายของสต๊อกรูม** ยังเป็นสีแบน ๆ เพราะเซสชันที่ทำไม่มีเครื่องมือสร้างไฟล์ภาพ
  และเลือกที่จะไม่ปลอมด้วย CSS gradient
- **ป้ายรัดสายสีส้ม** ทำได้แค่รูตาไก่ ยังไม่มีสายและยังไม่ห้อยล้ำขอบแผ่นป้าย
- **เสียงตัวอักษรที่สองสำหรับป้ายข้อมูล** — ตอนนี้ Chakra Petch ทำหน้าที่ทั้งหัวข้อย่อยและป้ายข้อมูล
- **กล่องเตือน `.alert-warn`** หลังถอดสีส้มออกแล้วยังไม่มีวัสดุอื่นมาแทน จึงดูเหมือนแผ่นป้ายธรรมดา
- **`index.html` แบบฉาก 3 มิติ** ยังไม่ได้ทำ รอให้เทสต์ผ่านครบก่อนจึงจะแตะโครงหน้าได้
